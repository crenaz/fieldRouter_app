#!/usr/bin/env bash
# bin/dev — Modern Rails 8 development runner

# Exit on error
set -e

# Start Rails server
rails server -p 3000 &

# Start JS watcher with ESBuild
npm run watch &

# Start TailwindCSS watcher
npm run watch:css &

# Wait for all background jobs to exit
wait